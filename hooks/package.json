{
    "name": "claude-hooks",
    "version": "1.0.0",
    "description": "TypeScript hooks for Claude Code - pre-bundled, zero runtime deps",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "bash ../scripts/build_hooks.sh --auto",
        "deploy": "../scripts/deploy_hooks.sh",
        "check": "tsc --noEmit",
        "dev": "npm run build && npm run check",
        "pretest": "npm run build",
        "test": "vitest run --reporter=verbose",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "@types/better-sqlite3": "^7.6.13",
        "@types/node": "^20.11.0",
        "esbuild": "^0.24.0",
        "typescript": "^5.3.3",
        "vitest": "^4.0.16"
    },
    "dependencies": {
        "better-sqlite3": "^12.5.0"
    }
}
