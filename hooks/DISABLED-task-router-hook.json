{
  "_comment": "DISABLED Task Router Hook - Routes Task tool calls to specialist agents via spawn_agent()",
  "_reason": "Agent spawning not working properly - agents run but don't create output files",
  "_re-enable": "Add this object back to PreToolUse array in .claude/settings.json",
  "_date_disabled": "2026-01-03",

  "hook_config": {
    "matcher": "Task",
    "hooks": [
      {
        "type": "command",
        "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/task-router.sh",
        "timeout": 60
      }
    ]
  }
}
